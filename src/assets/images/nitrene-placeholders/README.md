# NitreneGlog Image Placeholders

Put your custom images here later.

Suggested files:

- `avatar-nitrene.avif` for `src/config/profileConfig.ts`
- `banner-desktop.avif` for `src/config/backgroundWallpaper.ts`
- `banner-mobile.avif` for `src/config/backgroundWallpaper.ts`
- `logo-nitrene.avif` for `src/config/siteConfig.ts`

Use compressed `.avif`, `.webp`, or optimized `.jpg` files for better page speed.
